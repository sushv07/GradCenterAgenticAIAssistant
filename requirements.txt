# CSULB Grad Center AI Assistant – Streamlit frontend dependencies
#
# This file contains ONLY the packages needed to run app.py (Streamlit UI).
# It is intentionally lightweight so Streamlit Community Cloud can install it
# without pulling in the ~1 GB backend stack (torch, chromadb, LangChain).
#
# Backend dependencies (FastAPI, RAG, Chroma, sentence-transformers, etc.)
# are in requirements_backend.txt, which the Dockerfile installs instead.

# ── Web scraping (app.py imports tools/program_interest_tool → retrieval) ─────
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── Fuzzy matching (advisor retrieval) ───────────────────────────────────────
rapidfuzz>=3.0.0

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.35.0
