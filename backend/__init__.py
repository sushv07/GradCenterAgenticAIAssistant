"""
backend — unified backend entry point.

  entrypoint.py — handle_user_query(), the single function every caller
                  (Streamlit, tests, eval runners, and any future FastAPI
                  layer) should use to send a query into the backend.
"""
